<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"] - Student - Course Registration System</title>
    
    <!-- Bootstrap CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    
    <!-- Özel Stil -->
    <style>
        body {
            background-color: #2e2054; /* Koyu mor arka plan */
            color: #eee;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        header {
            background-color: #46267a; /* Orta mor ton */
            padding: 15px 0;
            box-shadow: 0 2px 8px rgba(0,0,0,0.5);
        }
        header .navbar-brand {
            color: #fff;
            font-weight: 600;
            font-size: 1.6rem;
            letter-spacing: 1px;
            margin-left: 0px; 
            transition: color 0.3s ease;
        }
        header .navbar-brand:hover {
            color: #a366d9;
            text-decoration: none;
        }
nav .nav-link {
   color: #fff !important;  
    font-weight: 500;
    font-size: 1.15rem;
    padding-left: 10px;
    padding-right: 10px;
    transition: color 0.3s ease, background-color 0.3s ease, box-shadow 0.3s ease;
}

nav .nav-link:hover,
nav .nav-link.active {
    background-color: rgba(163, 102, 217, 0.25); /* Hafif saydam mor kutu */
    color: #fff !important; /* Yazı beyaz */
    box-shadow: 0 0 8px 2px rgba(163, 102, 217, 0.8); /* Parlak glow efekti */
    border-radius: 8px;
    text-shadow: 0 0 8px rgba(255, 255, 255, 0.8); /* Yazıya parlaklık */
}


      
       /* Hover efektinde hafif şeffaf mor kutu ve kenarlık */
      nav .nav-link:hover {
    background: linear-gradient(145deg, rgba(163, 102, 217, 0.15), rgba(229, 193, 252, 0.15));
    border: 2px solid rgba(163, 102, 217, 0.6);
    color: #5a2980 !important; /* Okunaklı koyu mor */
    border-radius: 8px;
    padding: 6px 12px;
    text-decoration: none;
    transition: background 0.3s ease, border-color 0.3s ease, color 0.3s ease;
}



        /* Aktif sayfa için */
        nav .nav-link.active {
            background-color: rgba(163, 102, 217, 0.5);
            color: #a366d9 !important;
            font-weight: 700;
        }

        footer {
            background-color: #46267a;
            color: #ddd; 
            text-align: center;
            padding: 12px 0;
            position: fixed;
            width: 100%;
            bottom: 0;
            font-size: 0.9rem;
            letter-spacing: 1px;
            box-shadow: 0 -2px 8px rgba(0,0,0,0.5);
        }

        main.container {
            margin-bottom: 80px;
            padding-top: 25px;
        }

        /* Butonlar */
        .btn-primary {
            background-color: #7b3db1; /* Açık mor */
            border: none;
            color: #fff; 
            transition: background-color 0.3s ease;
        }
        .btn-primary:hover {
            background-color: #a366d9; 
        }

        /* Silme butonu kırmızı */
        .btn-danger {
            background-color: #ff5a5a;
            border: none;
            color: #fff;
            transition: background-color 0.3s ease;
        }
        .btn-danger:hover {
            background-color: #ff2a2a;
        }

        /* Navbar toggler */
        .navbar-toggler-icon {
            filter: none !important;
        }

        /* Başlık stili */
        h2.section-title {
            color: #a366d9;
            font-weight: 700;
            font-size: 2rem;
            margin-bottom: 20px;
            text-shadow: 1px 1px 3px #4b3176;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        /* Tablo mor tonlar */
        table {
            background-color: #46267a;
            color: #eee;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 0 15px rgba(0,0,0,0.5);
        }

        thead th {
            background: linear-gradient(90deg, #7b3db1, #5a2980);
            border-bottom: 2px solid #4a2d79;
            font-weight: 700;
        }

        tbody tr:nth-child(even) {
            background-color: #5a3ea5;
        }

        tbody tr:nth-child(odd) {
            background-color: #6e4bb2;
        }
        .btn-delete {
    background-color: #7a52a3 !important;
    border-color: #7a52a3 !important;
    color: white !important;
    padding: 8px 16px;
    border-radius: 5px;
    font-weight: bold;
    transition: background-color 0.3s ease;
}

.btn-delete:hover {
    background-color: #5c3673 !important;
    border-color: #5c3673 !important;
}

.btn-cancel {
    background-color: #6b5b7c !important;
    border-color: #6b5b7c !important;
    color: white !important;
    padding: 8px 16px;
    border-radius: 5px;
    font-weight: bold;
    transition: background-color 0.3s ease;
    margin-left: 10px;
}

.btn-cancel:hover {
    background-color: #4b3b5a !important;
    border-color: #4b3b5a !important;
}







        /* Responsive */
        @@media (max-width: 900px) {
            main.container {
                padding-left: 10px;
                padding-right: 10px;
            }
            nav .nav-link {
                font-size: 1rem;
            }
            h2.section-title {
                font-size: 1.6rem;
            }
        }
    </style>

</head>
<body>
    <header>
       <nav class="navbar navbar-expand-lg">
            <div class="container">
                <a class="navbar-brand" href="@Url.Action("Index", "Home")">Student - Course Registration System</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" 
                        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-3">
                        <li class="nav-item">
                            <a class="nav-link @(ViewData["ActivePage"]?.ToString() == "Home" ? "active" : "")" aria-current="page" href="@Url.Action("Index", "Home")">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link @(ViewData["ActivePage"]?.ToString() == "Students" ? "active" : "")" href="@Url.Action("Index", "StudentList")">Students</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link @(ViewData["ActivePage"]?.ToString() == "Teachers" ? "active" : "")" href="@Url.Action("Index", "Teacher")">Teachers</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link @(ViewData["ActivePage"]?.ToString() == "Courses" ? "active" : "")" href="@Url.Action("Index", "Course")">Courses</a>
                        </li>
                        <li class="nav-item">
                             <a class="nav-link @(ViewData["ActivePage"]?.ToString() == "StudentCourses" ? "active" : "")" href="@Url.Action("StudentCourses", "Application")">Student Courses</a>
                         </li>
                        <li class="nav-item">
                            <a class="nav-link @(ViewData["ActivePage"]?.ToString() == "About" ? "active" : "")" href="@Url.Action("About", "Home")">About</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link @(ViewData["ActivePage"]?.ToString() == "Help" ? "active" : "")" href="@Url.Action("Help", "Home")">Help</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link @(ViewData["ActivePage"]?.ToString() == "Security" ? "active" : "")" href="@Url.Action("Security", "Home")">Security</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <main class="container">
        @RenderBody()
    </main>

    <footer>
        &copy; 2025 Student - Course Registration System. All rights reserved.
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/js/bootstrap.bundle.min.js"></script>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
